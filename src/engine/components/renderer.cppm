module;
export module engine.components.renderer;
import engine.components;

namespace arbor {
    namespace engine {
        export class renderer : engine::component {
          public:
            ~renderer() = default;
        };
    } // namespace engine
} // namespace arbor